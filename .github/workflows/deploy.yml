name: Deploy to Vercel

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    types: [closed]

jobs:
  deploy:
    if: ${{ github.event_name == 'push' || (github.event.pull_request.merged && github.event.pull_request.base.ref == 'main') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build --if-present

      - name: Install Vercel CLI
        run: npm i -g vercel

      - name: Deploy to Vercel
        env:
          VERCEL_TOKEN: ylVfDlkCSJyEmIAx3IKrFIlX
          VERCEL_ORG_ID: pakin-kampeeras-projects
          VERCEL_PROJECT_ID: team_YFe0FsXFPGBTnzytHBPKSMGJ
        run: |
          vercel --prod --yes
